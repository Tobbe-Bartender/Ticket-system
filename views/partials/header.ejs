<!doctype html>
<html lang="en">
    <head>
        <title>Tech Help</title>
        <meta charset="utf-8">
        <link rel="stylesheet" href="/style/style.css">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    </head>
    <body>
        <p><img src="/img/Logo.png" alt="Logotyp"></p>
        <h1></h1>
        <ul class="nav">
            <li><a href="/">Home</a></li>
            
            <% if (oidc.isAuthenticated()) { %>
                <li><a href="/createticket">Create a ticket</a></li>
                <li><a href="/tickets">Tickets</a></li>
                    <% if (oidc.user['http://localhost:1337/roles'].includes('Agent')) { %>
                        <li><a href="/admin">Admin Panel</a></li>
                    <% } %>
                    <% if (oidc.user['http://localhost:1337/roles'].includes('Agent')) { %>
                        <li><a href="/knowledgebase">Knowledge Base</a></li>
                    <% } %>
            <% } %>
            

            <% if (oidc.isAuthenticated()) { %>
                <li style="margin-left: auto; display: flex; align-items: center;">
                    <a href="/logout" style="margin-right: 10px;">Logout</a>
                    <span style="font-size: 0.9em; color: #555;">Logged in as: <%= oidc.user.email %></span>
                </li>
            <% } else { %>
                <li class="nav-item" style="margin-left: auto;"><a href="/login">Login</a></li>
            <% } %>
        </ul>
    </body>
</html>
